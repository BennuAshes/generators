(function() {
    angular.module("app")
        .factory("RootStore",Factory);
        
        Factory.$inject = [];
        
        function Factory() {
            // contains the data for the whole application
            // a case: ng-repeat directive, attrs passed down
            var root = {
                
            };
            
            var factory = {
                
                
            };
            
            
            return factory;
        }
    
})();